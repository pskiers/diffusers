# @package _global_
defaults:
  - override /dataset: cifar100
  - override /model: unet2d

output_dir: "cifar100-conditional-standard"
train_batch_size: 256

model:
  num_class_embeds: 100

cfg_drop_rate: 0
guidance_scale: 0