# @package _global_
defaults:
  - override /dataset: cifar100
  - override /model: unet2d_trm

dataset:
  num_classes: null

# TRM loop settings
use_small_loop: true
T: 3
n: 6
N_supervision: 4

# Training hyperparameters
output_dir: "cifar100-size-small-nsup4-long"
train_batch_size: 256