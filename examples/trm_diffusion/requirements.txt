huggingface_hub>=0.27.0
kiwisolver==1.4.7
matplotlib==3.9.2
numpy==1.26.4
pillow==10.4.0
# PyYaml==0.2.5
hydra-core==1.3.2
jupyter-client==8.6.2
jupyter-core==5.7.2
jupyter-events==0.10.0
jupyter-lsp==2.2.5
jupyter-server==2.14.2
jupyter-server-terminals==0.5.3
jupyterlab==4.0.13
jupyterlab-pygments==0.3.0
jupyterlab-server==2.27.3
datasets==2.21.0
transformers==4.41.2
accelerate==0.34.2
mediapy
umap-learn
seaborn
opencv-python
wandb
pytest
hydra-core
wheel
setuptools
packaging
ninja
einops
timm
sentencepiece
adam-atan2
argdantic
zarr<3
h5py

# PushT
gym-pusht
pymunk<7
shapely
# PushT — lowdim (keypoints) variant, for the vendored 9-keypoint
gym==0.26.2
pygame
scikit-image
# ToolHang
robomimic==0.2.0
robosuite
# robosuite/mujoco_py's EGL renderer shells out to the `patchelf` binary to
# fix RPATHs on compiled .so files at env-creation time — the `patchelf`
# PyPI package ships a prebuilt binary (no compiler/root needed), it just
# needs to be pip-installed since most HPC clusters don't have it on PATH.
patchelf
# BlockPush — `gin-config`, `absl-py`, `six` are plain pip installs;
# `pybullet-svl` needs special handling (conflicts with plain pybullet, old
# sdist needs --no-build-isolation) and block_pushing itself is vendored, not
# pip installable — see README.md for the full sequence.
gin-config
absl-py
six

# TopoDiff — eval/topodiff_analysis.py (a verbatim vendored copy of the
# paper's own FEA-based compliance solver) calls solidspy's assembler API
# with the calling convention of solidspy<=1.0.16 (the latest release that
# existed when that paper code was written, Dec 2022). The current PyPI
# "latest" (1.1.0.post1, released Nov 2023) changed both DME's expected
# `cons` array shape and assembler's return arity, breaking that exact call
# — see eval/topodiff_eval.py's module docstring. Pin the old release, not
# "latest":
solidspy==1.0.16