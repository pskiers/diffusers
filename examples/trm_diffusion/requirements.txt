huggingface_hub>=0.27.0
kiwisolver==1.4.7
matplotlib==3.9.2
numpy==1.26.4
pillow==10.4.0
# PyYaml==0.2.5
hydra-core==1.3.2
jupyter-client==8.6.2
jupyter-core==5.7.2
jupyter-events==0.10.0
jupyter-lsp==2.2.5
jupyter-server==2.14.2
jupyter-server-terminals==0.5.3
jupyterlab==4.0.13
jupyterlab-pygments==0.3.0
jupyterlab-server==2.27.3
datasets==2.21.0
transformers==4.41.2
accelerate==0.34.2
mediapy
umap-learn
seaborn
opencv-python
scikit-image  # required by datasets/amaze/infer/maze_metrics.py (Amaze eval callback)
wandb
pytest
hydra-core
wheel
setuptools
packaging
ninja
flash_attn
einops
timm
sentencepiece
adam-atan2
argdantic
zarr<3
h5py

# ---------------------------------------------------------------------------
# Optional: closed-loop eval simulators for dp_eval_callbacks.py
# (PushT / BlockPush / ToolHang). NOT required for training — these callbacks
# gracefully skip (log a warning, return {}) if their simulator isn't
# installed. Only needed if you want the periodic/standalone closed-loop
# coverage/success-rate metrics.
# ---------------------------------------------------------------------------
# PushT:
gym-pusht
pymunk
shapely
# ToolHang (robomimic==0.2.0 matches upstream diffusion_policy's pin; newer
# robosuite releases have drifted API-wise from the version they used —
# see examples/trm_diffusion/README.md for the exact pinned robosuite fork/commit
# if you need closest fidelity to their reported numbers):
robomimic==0.2.0
robosuite
# BlockPush's `block_pushing` package is NOT on PyPI — it lives inside
# google-research/ibc and must be put on PYTHONPATH manually; see
# examples/trm_diffusion/README.md.