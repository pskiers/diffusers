# @package _global_
defaults:
  - override /hydra/launcher: basic
  - override /optimizer: adamw
  - override /dataset: sokoban
  - override /task: sokoban_uncond
  - override /model: sokoban_vit_trm

phases: [9000, 80000]
n_sup_phases: [1, 2]
# ~900k total steps:
# phases: [12000, 25000, 42000, 63000]
# n_sup_phases: [1, 2, 4, 8]

# num_epochs: 900 # 60 * 10^6 [original trained on 60mln images] / (1000*64) [samples in 1 epoch] ~= 900
# gradient_accumulation_steps: 12 # 64 * 12 = 768
num_epochs: 300
gradient_accumulation_steps: 1

save_model_epochs: 100
save_images_epochs: 100
train_batch_size: 64
epoch_max_batches_eval: 50
eval_batch_size: 50 # for validation in training
num_samples: 100
rescale_betas_zero_snr: true
clip_sample_range: 1.0
resume_from_checkpoint: latest

use_ddim: true
ddim_num_inference_steps: 50
ddpm_num_inference_steps: 400
output_dir: /net/tscratch/people/plgmgrzanka/training-outputs/diffusion/${hydra:runtime.choices.model}-${hydra:runtime.choices.task}

grad_every_n_sup: true
ema_max_decay: 0.999
mixed_precision: "bf16"
prediction_type: "sample"
