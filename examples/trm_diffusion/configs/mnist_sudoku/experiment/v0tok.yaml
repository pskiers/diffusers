# @package _global_
output_dir: "runs/mnist_sudoku_v0tok"

model:
  variant: "v0tok"
  kwargs:
    d_model: 512
    n_heads: 8
    n_layers: 2
    L_cycles: 6
    H_cycles: 3
    n_sup: 16
    num_puzzle_ids: 1000
    bridge_channels: 16
    painter_channels: [64, 128, 256]
    diff_thinker_weight: 1.0

train:
  sudoku_loss_weight: 1.0
