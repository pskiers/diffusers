# @package _global_
# TRM thinker + frozen BlockPush conv1d U-Net painter (ControlNet1D steering).
# Requires a trained blockpush_unet1d_painter checkpoint.
#
# Usage:
#   python train_trm.py experiment=blockpush_unet1d_thinker_controlnet \
#     painter.checkpoint=runs/blockpush_unet1d_painter/checkpoint_final.pt

defaults:
  - override /data: blockpush_lowdim
  - override /diffusion: ddpm_epsilon
  - override /backbone: blockpush_unet1d
  - override /painter: action_unet1d_controlnet1d_frozen_lowdim
  - override /thinker: action
  - override /translator: controlnet1d_small
  - override /condition_encoder: lowdim_obs_trm
  - override /train: action_thinker_lowdim
  - override /eval: action_thinker_lowdim
  - override /loss: mse
  - override /eval_callbacks: blockpush
  - override /sampling: action_direct
  - _self_

mode: "action_thinker_base"

run:
  output_dir: "runs/blockpush_unet1d_thinker_controlnet"
