# @package _global_
# Base diffusion model for PushT (lowdim obs), conv1d U-Net backbone.
# Usage: python train_trm.py experiment=pusht_lowdim_unet1d_painter
defaults:
  - override /data: pusht_lowdim
  - override /diffusion: ddpm_epsilon
  - override /backbone: pusht_lowdim_unet1d
  - override /painter: action_unet1d
  - override /condition_encoder: lowdim_obs
  - override /train: action_lowdim
  - override /eval: action_lowdim
  - override /eval_callbacks: pusht_lowdim
  - override /sampling: action_direct
  - _self_

mode: "painter_base"

run:
  output_dir: "runs/pusht_lowdim_unet1d_painter"
