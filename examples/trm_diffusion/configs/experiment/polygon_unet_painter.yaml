# @package _global_
# Unconditional Max-Area Polygon UNetPainter.
# Usage: python train_trm.py experiment=polygon_unet_painter

defaults:
  - override /data: polygon
  - override /backbone: polygon_unet
  - override /painter: polygon_unet
  - override /train: polygon
  - override /eval_callbacks: polygon
  - override /sampling: cfg
  - _self_

mode: "painter_base"

run:
  output_dir: "runs/polygon_unet_painter"

eval:
  step_timeout: 7200  # best-of-10 sampling + extraction/scoring across 3-4 point ranges
                       # measured up to ~60min total on the hardest range (41-50 pts) --
                       # extraction cost is O(n^2)-ish in vertex/point count, CPU-bound,
                       # independent of backbone size. Default (3600s) was too tight.
