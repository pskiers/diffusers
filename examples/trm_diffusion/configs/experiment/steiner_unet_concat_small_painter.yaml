# @package _global_
# Conditional Steiner UNetPainter (channel-concat, scaled-down backbone) — stage 1.
# Usage: python train_trm.py experiment=steiner_unet_concat_small_painter

defaults:
  - override /data: steiner
  - override /backbone: steiner_unet_concat_small
  - override /painter: steiner_unet_concat_small
  - override /train: steiner
  - override /eval_callbacks: steiner
  - override /sampling: cfg
  - _self_

mode: "painter_base"

run:
  output_dir: "runs/steiner_unet_concat_small_painter"

eval:
  step_timeout: 7200  # best-of-10 sampling + extraction/scoring across 3-4 point ranges
                       # measured up to ~60min total on the hardest range (41-50 pts) --
                       # extraction cost is O(n^2)-ish in vertex/point count, CPU-bound,
                       # independent of backbone size. Default (3600s) was too tight.
