# @package _global_
# TRM thinker + frozen BlockPush transformer painter (IP-Adapter steering).
# Requires a trained blockpush_transformer_painter checkpoint.
#
# Usage:
#   python train_trm.py experiment=blockpush_transformer_thinker_ipadapter \
#     painter.checkpoint=runs/blockpush_transformer_painter/checkpoint_final.pt

defaults:
  - override /data: blockpush_lowdim
  - override /diffusion: ddpm_epsilon
  - override /backbone: blockpush_transformer
  - override /painter: action_transformer_ipadapter_frozen_lowdim
  - override /thinker: action
  - override /translator: ipadapter_action
  - override /condition_encoder: lowdim_obs_trm
  - override /train: action_thinker_lowdim
  - override /eval: action_thinker_lowdim
  - override /loss: mse
  - override /eval_callbacks: blockpush
  - override /sampling: action_direct
  - _self_

mode: "action_thinker_base"

run:
  output_dir: "runs/blockpush_transformer_thinker_ipadapter"
