# @package _global_
# Conditional Polygon UNetPainter (channel-concat, scaled-down backbone) — stage 1.
# Usage: python train_trm.py experiment=polygon_unet_concat_small_painter

defaults:
  - override /data: polygon
  - override /backbone: polygon_unet_concat_small
  - override /painter: polygon_unet_concat_small
  - override /train: polygon
  - override /eval_callbacks: polygon
  - override /sampling: cfg
  - _self_

mode: "painter_base"

run:
  output_dir: "runs/polygon_unet_concat_small_painter"
