# MNIST eval callback: DDIM sampling + classifier accuracy (no thinker metrics).
- _target_: models.eval_callbacks.SudokuEvalCallback
  classifier_path: runs/mnist_classifier_cell16.pt
  cell_size: 16
  num_samples: 7680
  num_log_images: 8
