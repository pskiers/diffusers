# CLEVR noisy-image guidance wrapping CFG — V1 + linear blend toward V0.
_target_: models.sampling.SamplingPipeline
num_inference_steps: 20
batch_size: 256
predictor:
  _target_: models.sampling.NoisyGuidancePredictor
  inner:
    _target_: models.sampling.CFGPredictor
    scale: 2.0
  schedule:
    _target_: models.sampling.LinearSchedule
    noisy_end: 0.5
    clean_end: 1.0
