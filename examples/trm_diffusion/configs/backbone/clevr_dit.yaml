_target_: diffusers.Transformer2DModel
num_attention_heads: 8
attention_head_dim: 64       # inner_dim = num_attention_heads * attention_head_dim = 512
in_channels: 4
out_channels: 4
num_layers: 8
cross_attention_dim: 512     # must equal inner_dim for ada_norm_single
sample_size: 32
patch_size: 2
norm_type: "ada_norm_single"
attention_bias: true
activation_fn: "gelu-approximate"
caption_channels: 512
