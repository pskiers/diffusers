_target_: models.UnifiedConditionDiT
condition_mode: "spatial_concat"
sample_size: 32
# 4 noisy latent channels + 16 spatial mask channels
in_channels: 20
out_channels: 4
num_layers: 12
patch_size: 2
attention_head_dim: 64
num_attention_heads: 12
activation_fn: "gelu-approximate"
